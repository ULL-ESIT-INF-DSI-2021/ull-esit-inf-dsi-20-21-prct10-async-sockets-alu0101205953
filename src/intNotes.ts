/**
 * Interface that represents a note
 * @param title Title of the note
 * @param body Body of the note
 * @param color Color of the note
 */
export interface intNotes {
    title: string;
    body: string;
    color: string;
}
